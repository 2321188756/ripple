/** 主题模式：浅色 / 深色 / 跟随系统 */
export type Theme = "light" | "dark" | "system";

/** 设置面板 tab */
export type SettingsTab = "settings" | "logs" | "knowledge" | "stats" | "plugins";

/** 侧边栏 tab */
export type SidebarTab = "agents" | "chats" | "settings";
